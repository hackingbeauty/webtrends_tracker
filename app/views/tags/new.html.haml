- form_for :tag, @tag, :url => tags_path, :builder => LabeledFormBuilder, :html => { :id => 'create_tag', :class => 'rounded module', :multipart => true } do |f|
  = render :partial => "fields", :locals => { :f => f }
  
