#header
  - if logged_in?
    %ul
      %li= link_to "Logout", user_session_path, :method => :delete
      %li= link_to "All Tags", tags_path
      - products.each do |p|
        %li
          = link_to p.name, p